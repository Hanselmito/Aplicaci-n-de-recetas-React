services:
  api:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/db.json:/app/db.json
      - ./backend/uploads:/app/uploads
    environment:
      - PORT=3000
      - JWT_SECRET=mi-codigo-secreto